Products list:<br /><br />
<% if session[:cart].nil?%>
  session cart is empty
<% else %>
<%= session[:cart].to_s %>
<% end %>
<br />
<% @products.each do |product| %>
  <%= link_to product.name, product_path(product) %> |
  <%= product.description %> |
  <%= link_to 'Add to Cart', add_to_cart_product_path(product) %>
  <%= link_to 'Destroy', product_path(product), :method => :delete %><br />
<% end %>

<%= link_to "New Product", new_product_path %>
